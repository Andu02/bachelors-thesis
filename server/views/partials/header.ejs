<!DOCTYPE html>
<html lang="ro">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= title || "Aplicație" %></title>

  <!-- Bootstrap -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" />

  <!-- Stiluri -->
  <link rel="stylesheet" href="/styles/style.css" />
  <link rel="stylesheet" href="/styles/theme-change.css" />

  <!-- Aplica dark-mode inca din <head>, inainte de randarea paginii -->
  <script>
    if (localStorage.getItem("dark-mode") === "1") {
      document.documentElement.classList.add("dark-mode");
    }
  </script>

</head>
<body class="bg-light d-flex flex-column justify-content-center align-items-center min-vh-100">

    <!-- Buton Acasă -->
  <div class="position-absolute top-0 start-0 m-3">
    <a href="/logout" class="btn btn-outline-secondary">Acasă</a>
  </div>

  <!-- Comutator temă -->
  <%- include('theme-toggle') %>
